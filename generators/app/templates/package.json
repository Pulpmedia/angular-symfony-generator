{
  "name": "<%= name %>",
  "version": "0.0.0",
  "dependencies": {},
  "repository": {},
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-clean": "^0.6.0",
    "grunt-contrib-copy": "^0.7.0",
    "load-grunt-tasks": "^3.1.0",
    "time-grunt": "^1.0.0"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "install" : "cd <%= templateFolder %> && grunt build && cd .. && grunt build",
    "build": "cd <%= templateFolder %> && grunt build && cd .. && grunt build"
  }
}
